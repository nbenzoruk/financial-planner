# Financial Planner (FinPlan)

Финансовый планер для рационального принятия решений о покупках и инвестициях. Помогает избегать когнитивных искажений и принимать более осознанные финансовые решения.

## Возможности

- **Анализ покупок**: Расчет рентабельности, стоимости владения, стоимости одного использования
- **Инвестиционный анализ**: ROI, прогнозируемая доходность, риск-скорректированная доходность
- **Детекция когнитивных искажений**: Автоматическое обнаружение когнитивных ловушек при принятии финансовых решений
- **Хранение данных в Git**: Все данные в JSON формате, версионируются через Git

## Установка

```bash
cd financial-planner
npm install
npm run build
npm link  # Глобальная установка CLI команды
```

## Использование

### Управление покупками

#### Добавить покупку
```bash
finplan purchase add \
  -n "MacBook Pro" \
  -p 2500 \
  -c "Электроника" \
  -l 5 \
  -f daily \
  --notes "Ноутбук для работы"
```

**Опции:**
- `-n, --name <name>` - Название покупки (обязательно)
- `-p, --price <price>` - Цена (обязательно)
- `-c, --category <category>` - Категория (обязательно)
- `-l, --lifespan <years>` - Ожидаемый срок службы в годах
- `-m, --maintenance <cost>` - Стоимость обслуживания в год
- `-a, --alternative <cost>` - Альтернативная стоимость
- `-f, --frequency <freq>` - Частота использования (daily/weekly/monthly/rarely)
- `--notes <notes>` - Заметки

#### Анализ покупки
```bash
finplan purchase analyze <id>
```

#### Список всех покупок
```bash
finplan purchase list
finplan purchase list -c "Электроника"  # Фильтр по категории
```

### Управление инвестициями

#### Добавить инвестицию
```bash
finplan investment add \
  -n "S&P 500 ETF" \
  -a 10000 \
  -t stocks \
  -r 10 \
  --risk medium \
  --horizon 10 \
  --notes "Долгосрочные инвестиции"
```

**Опции:**
- `-n, --name <name>` - Название инвестиции (обязательно)
- `-a, --amount <amount>` - Начальная сумма (обязательно)
- `-t, --type <type>` - Тип: stocks/bonds/real-estate/crypto/business/other (обязательно)
- `-r, --return <percent>` - Ожидаемая доходность в год в % (обязательно)
- `--risk <level>` - Уровень риска: low/medium/high (обязательно)
- `--horizon <years>` - Срок инвестиции в годах (обязательно)
- `-c, --current <value>` - Текущая стоимость
- `--notes <notes>` - Заметки

#### Анализ инвестиции
```bash
finplan investment analyze <id>
```

#### Список всех инвестиций
```bash
finplan investment list
finplan investment list -t stocks  # Фильтр по типу
```

## Детектируемые когнитивные искажения

### Для покупок:
1. **Sunk Cost Fallacy** - Ошибка невозвратных затрат
2. **Lifestyle Creep** - Расползание образа жизни
3. **Anchoring Bias** - Эффект якоря
4. **Recency Bias** - Эффект недавности
5. **Loss Aversion** - Неприятие потерь

### Для инвестиций:
1. **FOMO** - Fear of Missing Out (страх упустить возможность)
2. **Confirmation Bias** - Предвзятость подтверждения
3. **Overconfidence** - Чрезмерная уверенность

## Примеры анализа

### Анализ покупки
```
=== Анализ рентабельности покупки ===

💰 Полная стоимость владения: 2500.00
📊 Стоимость одного использования: 1.37
📅 Эквивалент в день: 1.37

⚠️  ПРЕДУПРЕЖДЕНИЯ:
  • 🧠 LIFESTYLE CREEP: Рост расходов в категории...

💡 РЕКОМЕНДАЦИИ:
  • Рассмотрите более долговечные варианты...
```

### Инвестиционный анализ
```
=== Инвестиционный анализ ===

💵 Начальная сумма: 10000.00
🎯 Прогноз через 10 лет: 25937.42
📈 ROI: 159.37%
💹 Доходность (сложный процент): 159.37%
⚖️  Риск-скорректированная доходность: 5.00
🎲 Уровень риска: 🟡 medium
```

## Структура проекта

```
financial-planner/
├── src/
│   ├── analyzers/           # Модули анализа
│   │   ├── purchase-analyzer.ts
│   │   ├── investment-analyzer.ts
│   │   └── bias-detector.ts
│   ├── commands/            # CLI команды
│   │   ├── purchase.ts
│   │   └── investment.ts
│   ├── models/              # Типы данных
│   │   └── types.ts
│   ├── utils/               # Утилиты
│   │   └── storage.ts
│   └── index.ts             # Точка входа
├── data/                    # Хранилище данных (Git)
│   └── financial-data.json
└── package.json
```

## Хранение данных

Все данные хранятся в `data/financial-data.json`. Вы можете версионировать этот файл с помощью Git для отслеживания изменений в ваших финансах.

```bash
# Инициализация Git репозитория
cd financial-planner
git init
git add .
git commit -m "Initial commit"

# После добавления данных
git add data/financial-data.json
git commit -m "Added new purchase"
git push origin main  # Синхронизация с GitHub
```

## Разработка

```bash
# Режим разработки с hot reload
npm run dev

# Сборка
npm run build

# Проверка типов
npm run type-check
```

## Лицензия

MIT
